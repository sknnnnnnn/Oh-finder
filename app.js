const $=s=>document.querySelector(s),digits=v=>(v||"").replace(/\D/g,"");const lc=$("#loginCard"),sc=$("#searchCard"),rc=$("#resultCard");const setMsg=(e,t)=>e.textContent=t||"";$("#phone").addEventListener("input",e=>{const d=digits(e.target.value).slice(0,10);e.target.value=d.length<=3?d:d.length<=6?`${d.slice(0,3)}-${d.slice(3)}`:`${d.slice(0,3)}-${d.slice(3,6)}-${d.slice(6)}`});$("#loginBtn").onclick=async()=>{const b=$("#loginBtn"),username=$("#username").value.trim(),password=$("#password").value;if(!username||!password)return setMsg($("#loginMsg"),"กรุณากรอกรหัสพนักงานและรหัสผ่าน");b.disabled=true;setMsg($("#loginMsg"),"กำลังเข้าสู่ระบบ...");try{const r=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})});const j=await r.json();if(!r.ok||!j.ok)throw new Error(j.error||`HTTP ${r.status}`);$("#password").value="";setMsg($("#loginMsg"),"");lc.classList.add("hidden");sc.classList.remove("hidden");$("#phone").focus()}catch(e){setMsg($("#loginMsg"),e.message||"เข้าสู่ระบบไม่สำเร็จ")}finally{b.disabled=false}};$("#searchBtn").onclick=async()=>{const b=$("#searchBtn"),phone=digits($("#phone").value);if(!/^0\d{9}$/.test(phone))return setMsg($("#searchMsg"),"กรุณากรอกเบอร์ 10 หลัก");b.disabled=true;setMsg($("#searchMsg"),"กำลังค้นหา...");try{const r=await fetch(`/api/member?phone=${encodeURIComponent(phone)}`,{cache:"no-store"});const j=await r.json();if(r.status===401||r.status===403){sc.classList.add("hidden");lc.classList.remove("hidden")}if(!r.ok||!j.ok)throw new Error(j.error||`HTTP ${r.status}`);$("#memberName").textContent=j.member.name||"-";$("#memberCode").textContent=j.member.code;$("#qr").src=j.member.qr;setMsg($("#searchMsg"),"");sc.classList.add("hidden");rc.classList.remove("hidden")}catch(e){setMsg($("#searchMsg"),e.message||"ค้นหาไม่สำเร็จ")}finally{b.disabled=false}};$("#againBtn").onclick=()=>{rc.classList.add("hidden");sc.classList.remove("hidden");$("#phone").value="";setMsg($("#searchMsg"),"");$("#phone").focus()};$("#logoutBtn").onclick=async()=>{try{await fetch("/api/logout",{method:"POST"})}catch{}rc.classList.add("hidden");sc.classList.add("hidden");lc.classList.remove("hidden")};